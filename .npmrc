# Nx 23.1.1's React Native plugin caps its optional peer below the React Native
# 0.85.3 version required by the Expo 56 migration. Keep the generated SDK set
# installable until Nx widens that peer range.
legacy-peer-deps=true
