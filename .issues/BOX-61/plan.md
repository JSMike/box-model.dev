# Plan

1. Move the alert grid, visual surface, and close-control alignment into shadow-DOM wrappers.
2. Retain light-DOM paragraph normalization through the existing slot style service.
3. Add a regression test using fragmented projected content and a consumer host display override.
4. Rebuild and run the web package's type, lint, unit, build, and accessibility checks.
5. Refresh the package in the Angular CEM example and verify the rendered layout in a browser.

