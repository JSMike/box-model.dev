# Plan

1. Add a reflected `label` API to `progress-box`.
2. Apply the label as the accessible name of the internal progressbar.
3. Document the distinction between the accessible label and visible default-slot content.
4. Add unit and real-browser Axe coverage.
