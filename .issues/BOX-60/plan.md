# Plan

1. Secure Markdown rendering by disabling raw HTML, constraining generated attributes, and covering
   both raw-markup and Markdown-attribute injection.
2. Make shipped Sass styles self-contained and add an isolated-package compilation test.
3. Define single-delivery close/input event contracts and add count/target/type assertions.
4. Repair slot-only legends, WYSIWYG accessible names, and form-control examples.
5. Resolve the misleading `card-box` interaction name while the package is still unpublished.
6. Regenerate framework typings and the CEM, rebuild the package, and run all web-library checks.
